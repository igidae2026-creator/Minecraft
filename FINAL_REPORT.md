# RPG Network Release Manifest

- Velocity + Paper network rendered from centralized config
- Production backend bind and forwarding baseline enforced
- `rpg_core` upgraded into shared runtime service for profiles, guilds, sessions, audits, and persistence mirroring
- Economy, quests, dungeons, bosses, guilds, skills, events, and metrics plugins upgraded from skeletons to working runtime modules
- Local snapshot persistence + MySQL mirror + Redis live-session mirror added
- Deployment, backup, stop, healthcheck, validate, metrics, and scenario scripts added
- Plugin deployment matrix added for role-based backend separation
- Test coverage extended for ops, events, persistence, plugin command contracts, and runtime source wiring
